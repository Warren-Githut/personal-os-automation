---
name: generate-plan
description: Generate structured implementation plans from raw ideas.
version: 1.0.0
trigger: /generate-plan [requirement]
---

# /generate-plan
Formalize raw ideas into implementable plans.

## Position in workflow
`/brainstorm` or raw input -> `/generate-plan` -> `/review-plan` -> execution

## Behavior
- Convert vague requirements into structured components.
- Keep outputs review-ready.
